package com.example.android.discoroll;

public enum Genre {
    // in pairs of energy level, low and high

    // negative + still
    DEAD,
    PENTINGUP,

    // positive + still
    MEDITATING,
    HEARTRACING,

    // negative + danceable
    SWAYING,
    HEADBANGING,

    //  positive + danceable
    GROOVING,
    HIGH,
}
